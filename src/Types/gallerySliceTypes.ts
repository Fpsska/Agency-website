export interface galleryCardsTypes {
    id: string;
    category: string;
    text: string;
    image: string;
    alt_description: string;
    isActive: boolean;
}

export interface galleryNavTemplateTypes {
    id: string;
    category: string;
    text: string;
    isActive: boolean;
}
